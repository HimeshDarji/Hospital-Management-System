const mongoose = require('mongoose');
module.exports = mongoose.model('Laboratory', new mongoose.Schema({ reportNumber: { type: String, required: true, unique: true }, patient: { type: mongoose.Schema.Types.ObjectId, ref: 'Patient', required: true }, doctor: { type: mongoose.Schema.Types.ObjectId, ref: 'Doctor', default: null }, testName: { type: String, required: true, trim: true }, status: { type: String, enum: ['pending', 'in-progress', 'completed'], default: 'pending' }, requestedAt: { type: Date, default: Date.now }, completedAt: Date, reportUrl: String, result: String }, { timestamps: true }));
