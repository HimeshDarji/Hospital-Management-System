const router = require('express').Router(); const c = require('../controllers/appointmentController'); const { authenticate } = require('../middleware/authMiddleware'); const { authorize } = require('../middleware/roleMiddleware'); router.use(authenticate, authorize('admin')); router.route('/').get(c.list).post(c.create); router.route('/:id').get(c.get).patch(c.update).delete(c.remove); module.exports = router;
