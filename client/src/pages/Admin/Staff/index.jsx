import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'name',label:'Staff member'},{key:'email',label:'Email'},{key:'phone',label:'Phone'},{key:'role',label:'Role'},{key:'isVerified',label:'Verified',render:i=><span className="status-pill">{i.isVerified?'verified':'pending'}</span>}]; export default function Staff(){return <ResourcePage title="Staff" subtitle="View the doctors and reception team with access to MediSphere." endpoint="/admin/staff" columns={cols} fields={[]}/>}
