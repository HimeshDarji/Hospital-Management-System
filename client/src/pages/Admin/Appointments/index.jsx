import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'appointmentNumber',label:'Appointment'},{key:'patient',label:'Patient',render:i=>i.patient?.name},{key:'doctor',label:'Doctor',render:i=>i.doctor?.name},{key:'scheduledAt',label:'Date & time',render:i=>new Date(i.scheduledAt).toLocaleString()},{key:'status',label:'Status',render:i=><span className="status-pill">{i.status}</span>}]; const fields=[{key:'patient',label:'Patient ID',required:true},{key:'doctor',label:'Doctor ID',required:true},{key:'department',label:'Department ID',required:true},{key:'scheduledAt',label:'Date & time',type:'datetime-local',required:true},{key:'reason',label:'Reason',required:true},{key:'status',label:'Status',type:'select',options:['scheduled','confirmed','completed','cancelled','no-show']}]; export default function Appointments(){return <ResourcePage title="Appointments" subtitle="Book, reschedule, confirm and manage consultations." endpoint="/admin/appointments" columns={cols} fields={fields} filters={['scheduled','confirmed','completed','cancelled','no-show']}/>}
