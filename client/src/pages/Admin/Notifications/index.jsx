import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'title',label:'Notification'},{key:'message',label:'Message'},{key:'type',label:'Type'},{key:'isRead',label:'Status',render:i=><span className="status-pill">{i.isRead?'read':'unread'}</span>},{key:'createdAt',label:'Received',render:i=>new Date(i.createdAt).toLocaleString()}]; const fields=[{key:'title',label:'Title',required:true},{key:'message',label:'Message',required:true},{key:'type',label:'Type',type:'select',options:['appointment','system','billing','laboratory']}]; export default function Notifications(){return <ResourcePage title="Notifications" subtitle="System activity, reminders and team updates." endpoint="/admin/notifications" columns={cols} fields={fields}/>}
