import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'reportNumber',label:'Report no.'},{key:'patient',label:'Patient',render:i=>i.patient?.name},{key:'testName',label:'Test'},{key:'requestedAt',label:'Requested',render:i=>new Date(i.requestedAt).toLocaleDateString()},{key:'status',label:'Status',render:i=><span className="status-pill">{i.status}</span>}]; const fields=[{key:'patient',label:'Patient ID',required:true},{key:'doctor',label:'Doctor ID'},{key:'testName',label:'Test name',required:true},{key:'status',label:'Status',type:'select',options:['pending','in-progress','completed']},{key:'result',label:'Result'}]; export default function Laboratory(){return <ResourcePage title="Laboratory Reports" subtitle="Track pending tests, results and report delivery." endpoint="/admin/laboratory" columns={cols} fields={fields} filters={['pending','in-progress','completed']}/>}
