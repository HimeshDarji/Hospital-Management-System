import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'code',label:'Code'},{key:'name',label:'Department'},{key:'description',label:'Description'},{key:'headDoctor',label:'Head doctor',render:i=>i.headDoctor?.name},{key:'status',label:'Status',render:i=><span className="status-pill">{i.status}</span>}]; const fields=[{key:'name',label:'Department name',required:true},{key:'code',label:'Department code',required:true},{key:'description',label:'Description'},{key:'headDoctor',label:'Head Doctor ID'},{key:'status',label:'Status',type:'select',options:['active','inactive']}]; export default function Departments(){return <ResourcePage title="Departments" subtitle="Organize departments and assign clinical leadership." endpoint="/admin/departments" columns={cols} fields={fields} filters={['active','inactive']}/>}
