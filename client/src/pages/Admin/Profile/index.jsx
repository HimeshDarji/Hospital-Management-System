import useAuth from '../../../hooks/useAuth'; import { FiMail, FiPhone, FiUser } from 'react-icons/fi'; export default function Profile(){const {user}=useAuth();return <div className="admin-page"><div className="page-heading"><div><p>ADMIN PROFILE</p><h1>My profile</h1><span>Manage your MediSphere administrator account.</span></div></div><section className="panel profile-panel"><div className="profile-hero"><span>{user?.name?.[0]||'A'}</span><div><h2>{user?.name}</h2><p>System Administrator Â· {user?.isVerified?'Verified account':'Email verification pending'}</p></div></div><div className="profile-details"><p><FiMail/> {user?.email}</p><p><FiPhone/> {user?.phone||'No phone number provided'}</p><p><FiUser/> {user?.role}</p></div></section></div>}
