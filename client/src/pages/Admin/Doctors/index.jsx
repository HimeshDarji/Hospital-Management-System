import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'doctorId',label:'Doctor ID'},{key:'name',label:'Doctor'},{key:'specialization',label:'Specialization'},{key:'experience',label:'Experience',render:i=>`${i.experience||0} yrs`},{key:'availability',label:'Availability',render:i=><span className="status-pill">{i.availability}</span>}]; const fields=[{key:'name',label:'Full name',required:true},{key:'email',label:'Email',type:'email'},{key:'phone',label:'Phone',required:true},{key:'department',label:'Department ID',required:true},{key:'specialization',label:'Specialization',required:true},{key:'experience',label:'Experience',type:'number'},{key:'availability',label:'Availability',type:'select',options:['available','busy','off-duty']}]; export default function Doctors(){return <ResourcePage title="Doctors" subtitle="Coordinate physicians, departments and availability." endpoint="/admin/doctors" columns={cols} fields={fields} filters={['available','busy','off-duty']} filterKey="availability"/>}
