import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'patientId',label:'Patient ID'},{key:'name',label:'Patient'},{key:'phone',label:'Phone'},{key:'bloodGroup',label:'Blood group'},{key:'status',label:'Status',render:i=><span className="status-pill">{i.status}</span>}]; const fields=[['name','Full name',true],['email','Email','email'],['phone','Phone',true],['gender','Gender','select',['male','female','other','prefer-not-to-say']],['dob','Date of birth','date'],['bloodGroup','Blood group'],['address','Address']].map(([key,label,type,options])=>({key,label,type:type===true?'text':type,required:type===true,options})); export default function Patients(){return <ResourcePage title="Patients" subtitle="Manage patient profiles, records and medical history." endpoint="/admin/patients" columns={cols} fields={fields} filters={['active','inactive']}/>}
