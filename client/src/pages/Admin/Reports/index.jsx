import ResourcePage from '../../../components/tables/ResourcePage'; const cols=[{key:'title',label:'Report'},{key:'type',label:'Type'},{key:'period',label:'Period'},{key:'createdAt',label:'Generated',render:i=>new Date(i.createdAt).toLocaleDateString()}]; const fields=[{key:'title',label:'Report title',required:true},{key:'type',label:'Type',type:'select',options:['revenue','appointment','patient','doctor']},{key:'period',label:'Period',required:true}]; export default function Reports(){return <ResourcePage title="Reports" subtitle="Generate and export operational reports." endpoint="/admin/reports" columns={cols} fields={fields}/>}
