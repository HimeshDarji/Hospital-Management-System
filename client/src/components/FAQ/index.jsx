import { AnimatePresence, motion } from 'framer-motion'
import { FiMinus, FiPlus } from 'react-icons/fi'
import { useState } from 'react'

const faqs = [['Is MediSphere suitable for hospitals of different sizes?', 'Yes. MediSphere is designed to support independent practices, multi-specialty clinics and growing hospital groups with flexible, role-aware workflows.'], ['How is patient information protected?', 'We design every workflow around secure access, clear permissions and responsible handling of sensitive healthcare data.'], ['Can the platform be tailored to our workflows?', 'Absolutely. The platform is structured to accommodate the unique departments, processes and care pathways of your organization.'], ['How quickly can a team get started?', 'The clean, familiar interface helps teams learn quickly. A guided onboarding plan can be tailored to your organization.']]
function FAQ() { const [open, setOpen] = useState(0); return <section className="section faq-section"><div className="shell faq-grid"><div><p className="section-kicker">FAQ</p><h2>Questions, answered with clarity.</h2><p>Still curious? Our team is here to walk you through every detail.</p><a className="text-link" href="#contact">Talk to our team <span>→</span></a></div><div className="faq-list">{faqs.map(([question, answer], index) => <div className={`faq-item ${open === index ? 'open' : ''}`} key={question}><button type="button" onClick={() => setOpen(open === index ? -1 : index)}><span>{question}</span>{open === index ? <FiMinus /> : <FiPlus />}</button><AnimatePresence initial={false}>{open === index && <motion.div className="faq-answer" initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }}><p>{answer}</p></motion.div>}</AnimatePresence></div>)}</div></div></section> }
export default FAQ
